package com.company.ClassExercises.NewImplementation;

public class CheckOperation {
    static String check(int option){
        return option == 1 ? "addition" : option == 2 ? "subtraction" : "other operation";
    }
}
